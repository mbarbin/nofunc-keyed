(lang dunolint 1.0)

;; Everything is instrumented

(rule
 (enforce
  (dune
   (instrumentation
    (backend bisect_ppx)))))

(rule
 (cond
  ((path
    (glob src/**))
   (enforce
    (dune
     (preprocess no_preprocessing))))))
